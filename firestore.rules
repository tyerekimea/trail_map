rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Mobile and web clients should not access Firestore directly.
    // All data access must go through the backend API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
